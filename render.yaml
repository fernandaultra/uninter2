services:
  - type: web
    name: uninter-extensionista
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: GOOGLE_CREDENTIALS_JSON
        sync: false
